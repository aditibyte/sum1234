#include<stdio.h>
addition(int num);
main()
{
int num,sum;
	printf("Enter number:");
	scanf("%d",&num);
sum=addition(num);
	printf("Sum is %d",sum);
	return 0;
	
}
addition(int num)
{
	
if(num>0)
{
	
return ((num%10)+addition(num/10));
	}
else
{
return 0;
}
}
